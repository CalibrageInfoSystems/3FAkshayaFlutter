import 'package:flutter/material.dart';

class CropMaintenanceVisitsScreen extends StatelessWidget {
  const CropMaintenanceVisitsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Crop Maintenance Visits'),
      ),
    );
  }
}

// context.push(context.namedLocation(Routes.screenStory.name));